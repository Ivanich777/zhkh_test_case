import React from 'react';
import { PageTitle } from './Header.styled.tsx';

export const Header: React.FC = () => {
  return (
    <div>
      <PageTitle>Список счетчиков</PageTitle>
    </div>
  );
};
